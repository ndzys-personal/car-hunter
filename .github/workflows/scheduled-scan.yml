name: Car Hunter scheduled scan

on:
  workflow_dispatch:
  schedule:
    # Avoid GitHub Actions scheduler congestion at :00 and :30 (UTC).
    - cron: '7,37 * * * *'

jobs:
  scan:
    uses: ./.github/workflows/scan.yml
    secrets: inherit
